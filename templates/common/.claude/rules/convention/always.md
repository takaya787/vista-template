---
paths: "**/*"
---

## Owner

- Refer to `.vista/profile/me.json` for owner information (language, output format, autonomy; populated via `/onboarding`; if missing or skeleton, suggest running `/onboarding`)

## System File Language

System files (CLAUDE.md, rules/, skills/) **MUST** be written in English regardless of the owner's language preference. This ensures token efficiency and international accessibility.
